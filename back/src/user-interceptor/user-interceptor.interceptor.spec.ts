import { UserInterceptorInterceptor } from './user-interceptor.interceptor';

describe('UserInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new UserInterceptorInterceptor()).toBeDefined();
  });
});
